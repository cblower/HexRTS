
using UnityEngine;
using System.Collections;
using System.Collections.Generic;

[System.Serializable]
public struct Selection{
	/*
	public void GetDistance(Hex node1, Hex node2, bool distance){
		distance = (node1, node2) => 1;
	}
	Func<Hex, double> estimate = t => Math.Sqrt(Math.Pow(t.X - destination.X, 2) + Math.Pow(t.Y - destination.Y, 2)):
	*/
}